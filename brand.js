function brandHeader(){return `<header class="brand-bar"><img class="lockup" src="lockup.svg" alt="SPEEDY Mobile Auto Repair and Shop Services" /></header>`}function bottomNav(active){const tabs=[["index.html","Home"],["vendor.html","Vendor"],["rate.html","Rate"],["shop.html","Shop"]];return `<nav class="bottom">${tabs.map(([href,label])=>`<a href="${href}" class="${label===active?"on":""}">${label}</a>`).join("")}</nav>`}document.addEventListener("DOMContentLoaded",()=>{const header=document.getElementById("header");const nav=document.getElementById("nav");if(header)header.innerHTML=brandHeader();if(nav)nav.outerHTML=bottomNav(nav.dataset.active||"Home")});